import { FC } from "react"
import { Box } from "@mui/material"

const LoginScreen: FC = () => {
    return (
        <Box>
            <h1>Login Screen</h1>
        </Box>
    )
}

export default LoginScreen
